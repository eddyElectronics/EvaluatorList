## 🚀 Quick Start Guide

### ขั้นตอนที่ 1: ติดตั้งโปรเจค

```bash
# Clone และเข้าไปในโฟลเดอร์
cd evaluator

# ติดตั้ง dependencies
npm install --legacy-peer-deps
```

### ขั้นตอนที่ 2: ตั้งค่า Azure AD (ถ้ายังไม่ได้ทำ)

อ่านคู่มือใน [AZURE_SETUP.md](./AZURE_SETUP.md)

**สิ่งที่ต้องมี:**
- Azure AD Application (Client ID, Tenant ID, Client Secret)
- Redirect URI: `http://localhost:3000` (SPA)
- Permissions: `User.Read`, `User.ReadBasic.All`
- Employee ID ใน User Profile

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local`:

```env
NEXT_PUBLIC_AZURE_AD_CLIENT_ID=your-client-id
AZURE_AD_CLIENT_SECRET=your-client-secret
NEXT_PUBLIC_AZURE_AD_TENANT_ID=your-tenant-id
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=https://api.airportthai.co.th/API2/eva
```

### ขั้นตอนที่ 4: รันโปรเจค

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่: **http://localhost:3000**

---

## 📖 คู่มือการใช้งาน

### 1. เข้าสู่ระบบ

![Login Screen](docs/login-screen.png)

1. เปิด http://localhost:3000
2. คลิกปุ่ม **"เข้าสู่ระบบ"**
3. Popup จะเปิดขึ้นสำหรับ Azure AD Login
4. กรอก Email และ Password ของบริษัท
5. ระบบจะ redirect กลับมาหลังจาก login สำเร็จ

### 2. หน้าหลัก - รายการผู้ประเมิน

![Main Screen](docs/main-screen.png)

หลังจาก login สำเร็จ คุณจะเห็น:

#### ส่วนบน (Header)
- **ชื่อหัวข้อ**: "กำหนดรายชื่อผู้ประเมิน"
- **User Profile Card**: 
  - รูปโปรไฟล์
  - ชื่อ-นามสกุล
  - ตำแหน่ง
  - รหัสพนักงาน
  - ปุ่ม "ออกจากระบบ"

#### ส่วนกลาง (ตาราง)
แสดงรายการผู้ประเมินทั้งหมด:

| คอลัมน์ | คำอธิบาย |
|---------|----------|
| ผู้ถูกประเมิน | ชื่อคนที่ถูกประเมิน |
| หน่วยงาน | แผนก/ฝ่าย |
| ตำแหน่ง | ตำแหน่งงาน |
| ผู้ประเมิน 1 | ชื่อผู้ประเมินคนที่ 1 |
| ผู้ประเมิน 2 | ชื่อผู้ประเมินคนที่ 2 |
| ผู้ประเมิน 3 | ชื่อผู้ประเมินคนที่ 3 |
| จัดการ | ปุ่ม "แก้ไข" |
| ผู้บันทึก | คนที่บันทึกล่าสุด |
| เวลาบันทึก | วันเวลาที่บันทึก |

#### ปุ่มด้านบน
- **รีเฟรชข้อมูล**: โหลดข้อมูลใหม่จาก API

### 3. แก้ไขผู้ประเมิน

![Edit Modal](docs/edit-modal.png)

1. คลิกปุ่ม **"แก้ไข"** ในแถวที่ต้องการ
2. Modal (Popup) จะเปิดขึ้น
3. คุณจะเห็น:
   - **ผู้ถูกประเมิน**: แสดงชื่อ (ไม่สามารถแก้ไขได้)
   - **ผู้ประเมิน 1**: Dropdown เลือกผู้ประเมินคนที่ 1
   - **ผู้ประเมิน 2**: Dropdown เลือกผู้ประเมินคนที่ 2
   - **ผู้ประเมิน 3**: Dropdown เลือกผู้ประเมินคนที่ 3

#### การใช้ Dropdown
- **พิมพ์เพื่อค้นหา**: พิมพ์รหัสพนักงานหรือชื่อ
- **เลือกจากรายการ**: คลิกเพื่อเลือก
- **ลบการเลือก**: คลิก ✕ ที่ด้านขวา
- **ใช้คีย์บอร์ด**: ↑↓ เลือก, Enter ยืนยัน, Esc ปิด

4. เมื่อเลือกเสร็จแล้ว คลิกปุ่ม **"บันทึก"**
5. ระบบจะแสดง Alert "บันทึกข้อมูลสำเร็จ"
6. Modal ปิดอัตโนมัติ
7. ตารางจะอัพเดทด้วยข้อมูลใหม่

#### การยกเลิก
- คลิกปุ่ม **"ปิด"**
- หรือคลิกนอก Modal (พื้นที่สีดำ)

### 4. ออกจากระบบ

1. คลิกปุ่ม **"ออกจากระบบ"** ที่ User Profile Card
2. ระบบจะ logout และกลับไปหน้า Login

---

## 🔍 คำอธิบายเพิ่มเติม

### Autocomplete Dropdown

Dropdown สำหรับเลือกผู้ประเมินมีความสามารถพิเศษ:

✅ **ค้นหาได้**: พิมพ์ชื่อหรือรหัสพนักงานเพื่อค้นหา  
✅ **Auto-complete**: แสดงผลลัพธ์ทันทีขณะพิมพ์  
✅ **แสดงทั้งรหัสและชื่อ**: ง่ายต่อการหา  
✅ **Scroll**: เลื่อนดูรายชื่อได้  
✅ **Clear**: ลบการเลือกได้ง่าย  

รูปแบบการแสดงผล:
```
EMP001 - นาย ก ส.
EMP002 - นางสาว ข ย.
EMP003 - นาง ค ว.
```

### การโหลดข้อมูล

ระบบจะโหลดข้อมูลอัตโนมัติเมื่อ:
- Login สำเร็จ
- บันทึกข้อมูลเสร็จ
- กดปุ่ม "รีเฟรชข้อมูล"

ขณะโหลด จะมี **Loading Indicator** (Spinner) แสดง

### การจัดการ Session

- **Session**: จัดเก็บใน sessionStorage
- **หมดอายุ**: เมื่อปิด Browser tab
- **ต่ออายุ**: อัตโนมัติขณะใช้งาน
- **ปลอดภัย**: Token ไม่ถูกเก็บแบบถาวร

---

## ⚠️ ข้อควรระวัง

### 🔴 สิ่งที่ต้องตรวจสอบก่อนใช้งาน

1. **Employee ID**
   - ต้องมี Employee ID ใน Azure AD User Profile
   - ถ้าไม่มี จะไม่สามารถดึงข้อมูลได้

2. **Permissions**
   - ต้อง Grant admin consent ใน Azure AD
   - ถ้าไม่ได้ Grant จะ login ไม่ได้

3. **Redirect URI**
   - ต้องตั้งค่าใน Azure AD ตรงกับที่ใช้
   - Development: `http://localhost:3000`
   - Production: `https://yourdomain.com`

4. **Network**
   - ต้องสามารถเชื่อมต่อ internet ได้ (สำหรับ Azure AD)
   - ต้องสามารถเข้าถึง Backend API ได้

### 🟡 ข้อจำกัด

- รองรับเฉพาะ **Modern Browsers** (Chrome, Edge, Firefox, Safari)
- ต้องเปิด **JavaScript**
- ต้องเปิด **Cookies** สำหรับ Azure AD
- ต้องเปิด **Popup** สำหรับ Login

---

## 🆘 Troubleshooting

### ปัญหา: Login ไม่ได้

**อาการ**: กด "เข้าสู่ระบบ" แล้วไม่มีอะไรเกิดขึ้น

**วิธีแก้**:
1. ตรวจสอบ Browser Console (F12)
2. ดูว่ามี Error message อะไร
3. ตรวจสอบว่า Popup ไม่ถูกบล็อก
4. ลองใช้ Incognito mode
5. Clear browser cache และ cookies

### ปัญหา: ไม่มีข้อมูลในตาราง

**อาการ**: Login ได้แต่ตารางว่างเปล่า

**วิธีแก้**:
1. เปิด Browser Console (F12) → Network tab
2. ตรวจสอบว่า API `/api/eva/get-data` ถูกเรียกหรือไม่
3. ดู Response ของ API
4. ตรวจสอบว่า Employee ID ถูกส่งไปหรือไม่
5. ลอง Refresh หน้าเว็บ (F5)

### ปัญหา: Dropdown ไม่แสดงรายชื่อ

**อาการ**: Modal เปิดได้แต่ Dropdown ว่างเปล่า

**วิธีแก้**:
1. เปิด Browser Console (F12) → Network tab
2. ตรวจสอบว่า API `/api/employees` ถูกเรียกหรือไม่
3. ดู Response ว่ามีข้อมูลหรือไม่
4. ตรวจสอบว่า Backend API `getEmployeeERP` ทำงานหรือไม่
5. Refresh หน้าเว็บ

### ปัญหา: บันทึกไม่สำเร็จ

**อาการ**: กด "บันทึก" แล้วมี Error

**วิธีแก้**:
1. ดู Error message ที่แสดง
2. เปิด Browser Console (F12)
3. ตรวจสอบ Network tab ดู API `/api/eva/save-data`
4. ตรวจสอบ Request body ที่ส่งไป
5. ตรวจสอบว่า Backend API `EvaSaveData` ทำงานหรือไม่

### ปัญหา: รูปโปรไฟล์ไม่แสดง

**อาการ**: ชื่อมีแต่ไม่มีรูป

**วิธีแก้**:
- ไม่ใช่ปัญหา! บาง User ไม่มีรูปใน Azure AD
- ระบบจะแสดงตัวอักษรแรกของชื่อแทน (เช่น **ส** สำหรับ สมชาย)

---

## 📞 ติดต่อ Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. **Technical Issues**: เปิด Issue ใน GitHub
2. **Azure AD Issues**: ติดต่อ IT Admin
3. **Backend API Issues**: ติดต่อ Backend Team
4. **Feature Requests**: เปิด Discussion ใน GitHub

---

## 📚 เอกสารเพิ่มเติม

- **[README.md](./README.md)** - ภาพรวมและการติดตั้ง
- **[AZURE_SETUP.md](./AZURE_SETUP.md)** - คู่มือตั้งค่า Azure AD
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - โครงสร้างระบบและเทคนิค

---

## 🎯 Tips & Tricks

### เพิ่มความเร็วในการค้นหา
- พิมพ์ **รหัสพนักงาน** ถ้าจำได้ (เช่น `EMP001`)
- พิมพ์ **ชื่อย่อ** ถ้าจำไม่ได้ (เช่น `สม` สำหรับ สมชาย)

### แก้ไขหลายคนพร้อมกัน
- เปิดหลาย tab แล้วแก้ไขพร้อมกัน
- บันทึกทีละคน
- Refresh tab อื่นๆ เพื่อดูข้อมูลล่าสุด

### Keyboard Shortcuts
- `F5` - Refresh หน้าเว็บ
- `F12` - เปิด Developer Tools
- `Ctrl+F` - ค้นหาในตาราง (ใช้ของ Browser)

---

## ✅ Checklist สำหรับ Admin

เมื่อ setup ระบบใหม่ ตรวจสอบดังนี้:

### Azure AD
- [ ] สร้าง App registration แล้ว
- [ ] ตั้งค่า Redirect URI แล้ว
- [ ] เพิ่ม API Permissions แล้ว
- [ ] Grant admin consent แล้ว
- [ ] All users มี Employee ID แล้ว

### Environment
- [ ] สร้างไฟล์ `.env.local` แล้ว
- [ ] ใส่ Client ID ถูกต้อง
- [ ] ใส่ Tenant ID ถูกต้อง
- [ ] ใส่ Client Secret ถูกต้อง
- [ ] ใส่ API Base URL ถูกต้อง

### Testing
- [ ] ทดสอบ Login สำเร็จ
- [ ] ทดสอบแสดงรายการได้
- [ ] ทดสอบแก้ไขและบันทึกได้
- [ ] ทดสอบ Dropdown แสดงรายชื่อ
- [ ] ทดสอบ Logout สำเร็จ

### Production
- [ ] เปลี่ยน Redirect URI เป็น Production URL
- [ ] ตั้งค่า Environment Variables ใน Server
- [ ] เปลี่ยน API Base URL เป็น Production (ถ้ามี)
- [ ] ทดสอบ Production environment
- [ ] Backup database ของ Backend

---

**Happy Evaluating! 🎉**
